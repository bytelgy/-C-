任务描述
本关任务：编写一个程序，输入百分制成绩（整数），输出对应的等级分制成绩。

相关知识
为了完成本关任务，你需要掌握：

选择结构i语句 百分制成绩grade和等级分制成绩的对应关系是： 90（含）到100（含）之间：A 80（含）到90（不含）之间：B 70（含）到80（不含）之间：C 60（含）到70（不含）之间：D 60（不含）以下：F
编程要求
在右侧编辑器填写代码。输入百分制成绩，输出对应的等级分制成绩。

输入形式
一个整数，为百分制成绩。

输出形式
百分制成绩对应的等级分制成绩。

测试说明
平台会对你编写的代码进行测试：

样例输入
85

样例输出
B


代码示例：
#include <stdio.h>
#include <stdlib.h>

int main()
{
	int grade;
	scanf("%d",&grade);
	
	if (90<=grade&&grade<=100)
	{
		printf("A");
	}
	else if (80<=grade&&grade<90)
	{
		printf("B");
	}
	else if (70<=grade&&grade<80)
	{
		printf("C");
	}
	else if (60<=grade&&grade<70)
	{
		printf("D");
	}
	else
	{
		printf("F");
	}
}
