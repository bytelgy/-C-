任务描述
本关任务：编写一个程序，输入百分制成绩，判断是否及格。

相关知识
为了完成本关任务，你需要掌握：

选择结构if-else语句
编程要求
在右侧编辑器填写代码。输入一个百分制成绩，输出是否及格的信息。及格输出 "Passed!"，不及格输出"Failed!"。

输入形式
在提示信息"Please input a grade:"的下一行输入一个整数，

输出形式
输出是否及格的信息，及格输出 "Passed!"，不及格输出"Failed!"。

测试说明
平台会对你编写的代码进行测试：

样例输入/输出
（带下划线的数据为用户输入）

Please input a grade:
90
Passed!


代码示例：
#include <stdio.h>
#include <stdlib.h>

int main()
{
	int number;
	printf("Please input a grade:\n");
	scanf("%d",&number);	
	if (number>=60)
		{
			printf("Passed!");
		}
	else
		{
			printf("Failed!");
		}
	
	return 0;
} 
