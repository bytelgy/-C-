任务描述
本关任务：写一个程序，读入五个整数，然后确定其中的最大值和最小值并打印结果。

相关知识
为了完成本关任务，你需要掌握：

选择结构if语句
求最大最小的方法
可以使用“打擂台”的方式求最大值和最小值。

编程要求
在右侧编辑器填写代码。读入五个整数，然后确定其中的最大值和最小值并打印结果。

输入形式
在提示信息"Input 5 integers:"的下一行输入5个整数。

输出形式
两行。
第1行，在提示信息"The largest value is "后输出最大值。
第2行，在提示信息"The smallest value is "后输出最小值。

测试说明
平台会对你编写的代码进行测试：

样例输入/输出1
（带下划线的数据为用户输入）

Input 5 integers:
2 3 1 5 4
The largest value is 5
The smallest value is 1


代码示例：
#include<stdio.h>
#include<math.h>
int max(int x,int y)
{
if(x>y)
return x;
else
return y;
}
int min(int x,int y)
{
if(x<y)
return x;
else
return y;
}
int main()
{
int x,y,a,b,c,d,e,f,g,h,i,t,o,p,l;printf("Input 5 integers:\n");scanf("%d%d%d%d%d",&a,&b,&c,&d,&e);
f=max(a,b);
g=max(f,c);
h=max(g,d);
i=max(h,e);
t=min(a,b);
o=min(t,c);
p=min(o,d);
l=min(p,e);
printf("The largest value is %d\nThe smallest value is %d",i,l);
}
